from setuptools import find_packages, setup

setup(
    name="atlanta_crime",
    version="0.0.1",
    description="Dependencies for a project in a Python package",
    packages=find_packages(),
    python_requires='>=3.8',
)